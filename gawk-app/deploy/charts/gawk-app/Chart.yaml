apiVersion: v2
name: gawk-app
description: gawk frontend — the broadcast/view SPA served by nginx.
type: application
# Both fields are bumped by release-please; the image tag defaults to
# appVersion, so chart version == image version always holds.
version: 0.7.1 # x-release-please-version
appVersion: "0.7.1" # x-release-please-version
