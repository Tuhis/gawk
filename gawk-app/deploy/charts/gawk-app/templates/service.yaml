apiVersion: v1
kind: Service
metadata:
  name: {{ include "gawk-app.fullname" . }}
  labels:
    {{- include "gawk-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "gawk-app.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
