apiVersion: v2
name: gawk-server
description: WebTransport relay for the gawk game stream — one publisher fans out encoded video datagrams to a small set of subscribers.
type: application
# Both fields are bumped by release-please; the image tag defaults to
# appVersion, so chart version == image version always holds.
version: 0.20.1 # x-release-please-version
appVersion: "0.20.1" # x-release-please-version
